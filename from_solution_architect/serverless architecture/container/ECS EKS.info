ECS
    ECS does not work on premises
    easy to setup and integrate with all of the other services available in AWS

EKS
    best to use. when you are not all in on aws.
    
    open source alternative
    can be used to on premises and cloud 
    aws managed version is called elastic kubernetes service


ECS steps to create and launch containers

1. Task definitions
2. Launch type
3. give some resources and configure aws IAM roles if needed
4. add container. which is uploaded on ECR

now create a cluster
where the kubernetes cluster is going to be deployed. where kube api and kube api server and all the other components of the cluster woulc be deployed.


run in new task.
the task that we created. needed to be deployed. 